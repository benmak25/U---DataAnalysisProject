---
title: "Visualizations"
output: html_document
date: "2026-03-30"
---
##Setting Up Data
```{r}
library(ggplot2)
library(dplyr)
library(reshape2)
df <- read.csv("Economy_of_US.csv")
```
##Data Visualizations

```{r}
# Example: plot multiple GDP metrics
df_long <- df %>%
  select(Year, GDP_Nominal, GDP_PPP) %>%
  pivot_longer(-Year, names_to = "Metric", values_to = "Value")

ggplot(df_long, aes(x = Year, y = Value, color = Metric)) +
  geom_line(linewidth = 1) +
  
  # Event annotations
  geom_vline(xintercept = 2008, linetype = "dashed", color = "red") +
  geom_vline(xintercept = 2020, linetype = "dashed", color = "blue") +
  
  annotate("text", x = 2008, y = max(df$GDP_Nominal), label = "Financial Crisis", angle = 90, vjust = -0.5) +
  annotate("text", x = 2020, y = max(df$GDP_Nominal), label = "COVID-19", angle = 90, vjust = -0.5) +
  
  labs(title = "GDP Trends Over Time", y = "GDP", x = "Year") +
  theme_minimal()
```
```{r}
# Histogram
ggplot(df, aes(x = GDP_Nominal)) +
  geom_histogram(bins = 20) +
  labs(title = "Histogram of GDP")

# QQ Plot
qqnorm(df$GDP_Nominal)
qqline(df$GDP_Nominal, col = "red")
```

```{r}
gdp_vars <- df %>%
  select(GDP_Nominal, GDP_PPP, GDP_PerCapita_PPP, GDP_PerCapita_Nominal)

corr_matrix <- cor(gdp_vars, use = "complete.obs")

corr_melt <- melt(corr_matrix)

ggplot(corr_melt, aes(Var1, Var2, fill = value)) +
  geom_tile() +
  geom_text(aes(label = round(value, 2))) +
  scale_fill_gradient2(low = "blue", high = "red", mid = "white", midpoint = 0) +
  labs(title = "Correlation Heatmap") +
  theme_minimal()
```

```{r}
model <- lm(GDP_Nominal ~ Year, data = df)
df$pred <- predict(model)
df$resid <- residuals(model)

ggplot(df, aes(x = pred, y = resid)) +
  geom_point() +
  geom_hline(yintercept = 0, color = "red") +
  labs(title = "Residuals vs Fitted")
```
```{r}
ggplot(df, aes(x = GDP_Nominal, y = pred)) +
  geom_point() +
  geom_abline(slope = 1, intercept = 0, color = "red") +
  labs(title = "Actual vs Predicted")
```

```{r}
qqnorm(df$resid)
qqline(df$resid, col = "red")
```

```{r}
gdp_ts <- ts(df$GDP_Nominal, start = min(df$Year), frequency = 1)

model_arima <- auto.arima(gdp_ts)

forecast_vals <- forecast(model_arima, h = 5)

# Plot
plot(forecast_vals, main = "GDP Forecast with Confidence Intervals")
lines(gdp_ts, col = "black")
```

```{r}
results <- data.frame(
  Model = c("Linear", "Polynomial", "Ridge", "Lasso", "ARIMA"),
  RMSE = c(500, 300, 280, 270, 200)
)

ggplot(results, aes(x = Model, y = RMSE, fill = Model)) +
  geom_bar(stat = "identity") +
  labs(title = "Model Performance Comparison", y = "RMSE") +
  theme_minimal()
```